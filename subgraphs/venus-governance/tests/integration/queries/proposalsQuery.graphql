query Proposals {
  proposals {
      id
      proposer {
        id
      }
      targets
      values
      signatures
      calldatas
      startBlock
      endBlock
      description
      executionEta
      queued
      canceled
      executed
      votes {
        id
        votes
        support
      }
    }
}
