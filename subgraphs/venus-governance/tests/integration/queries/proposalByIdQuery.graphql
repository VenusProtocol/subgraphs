query ProposalById($id: ID!) {
  proposal(id: $id) {
      id
      proposer {
        id
      }
      targets
      values
      signatures
      calldatas
      startBlock
      endBlock
      description
      status
      executionETA
      votes {
        id
        votes
        support
      }
    }
}
