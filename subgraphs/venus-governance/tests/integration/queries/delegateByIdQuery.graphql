query DelegateById($id: ID!) {
  delegate(id: $id) {
      id
      stakedXvsMantissa
      totalVotesMantissa
      delegateCount
      delegates {
        id
      }
      votes {
        id
        support
        votes
        proposal {
          id
        }
      }
      proposals {
        id
      }
    }
}
